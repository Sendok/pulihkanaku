# Route Map

## Public

`/`, `/cara-kerja`, `/untuk-pekerja`, `/untuk-bisnis`, `/keamanan`, `/harga`, `/tentang`, `/bantuan`, `/kontak`, `/pekerjaan`, `/pekerjaan/[slug]`, `/kota/[slug]`, `/kategori/[slug]`, dan halaman kebijakan.

## Worker

`/app`, `/app/pekerjaan`, `/app/aktivitas`, `/app/pesan`, `/app/pendapatan`, `/app/rencana-penghasilan`, `/app/profil`.

## Business

`/business`, `/business/jobs`, `/business/jobs/new`, `/business/candidates`, `/business/shifts`, `/business/payments`, `/business/settings`.

## Admin

`/admin` dan resources users, workers, businesses, jobs, applications, assignments, payments, payouts, verifications, disputes, reports, safety flags, content, locations, categories, skills, pricing, flags, audit, health, dan settings.
