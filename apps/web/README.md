# Web application

Surface web vinext/Next yang aktif masih berada di root repository agar deployment Sites tetap kompatibel selama migrasi bertahap. Pemindahan fisik ke `apps/web` dilakukan setelah service API PostgreSQL mencapai parity dengan D1 hosted preview.
