import{BadRequestException,Body,Controller,ForbiddenException,Get,Inject,Injectable,Param,Patch,Post,Req}from"@nestjs/common";import{IsIn,IsInt,IsOptional,IsString,Matches,MaxLength,Min,MinLength}from"class-validator";import type{Request}from"express";import type{Pool}from"pg";import{POSTGRES}from"../infrastructure.module.js";import{AuthService}from"./auth.js";
const contentRoles=new Set(["OPERATIONS_ADMIN","SUPER_ADMIN"]);export class CreateContentDto{@Matches(/^[a-z0-9]+(?:-[a-z0-9]+)*$/)slug!:string;@IsIn(["GUIDE","FAQ","ANNOUNCEMENT"])type!:string;@IsString()@MinLength(5)@MaxLength(180)title!:string;@IsString()@MinLength(10)@MaxLength(500)summary!:string;@IsString()@MinLength(20)@MaxLength(30_000)body!:string;@IsOptional()@IsIn(["DRAFT","PUBLISHED"])status?:string}export class UpdateContentDto{@IsInt()@Min(1)expectedVersion!:number;@IsOptional()@IsString()@MinLength(5)@MaxLength(180)title?:string;@IsOptional()@IsString()@MinLength(10)@MaxLength(500)summary?:string;@IsOptional()@IsString()@MinLength(20)@MaxLength(30_000)body?:string;@IsOptional()@IsIn(["DRAFT","PUBLISHED","ARCHIVED"])status?:string}
@Injectable()export class ContentService{constructor(@Inject(POSTGRES)private readonly pool:Pool,private readonly auth:AuthService){}async published(){return{items:(await this.pool.query(`select slug,type,title,summary,published_at,updated_at from content_entries where status='PUBLISHED' order by published_at desc`)).rows}}async entry(slug:string){const row=(await this.pool.query(`select slug,type,title,summary,body,published_at,updated_at from content_entries where slug=$1 and status='PUBLISHED'`,[slug])).rows[0];if(!row)throw new BadRequestException("Konten tidak ditemukan.");return row}private async admin(req:Request){const actor=await this.auth.sessionFrom(req);if(!contentRoles.has(actor.role))throw new ForbiddenException();return actor}async adminList(req:Request){await this.admin(req);return{items:(await this.pool.query(`select id,slug,type,title,summary,status,published_at,version,updated_at from content_entries order by updated_at desc`)).rows}}async create(req:Request,body:CreateContentDto){const actor=await this.admin(req);try{return(await this.pool.query(`insert into content_entries(slug,type,title,summary,body,status,published_at,author_user_id) values($1,$2,$3,$4,$5,$6,case when $6='PUBLISHED' then now() else null end,$7) returning *`,[body.slug,body.type,body.title.trim(),body.summary.trim(),body.body.trim(),body.status??"DRAFT",actor.user_id])).rows[0]}catch(error){if((error as{code?:string}).code==="23505")throw new BadRequestException("Slug sudah digunakan.");throw error}}async update(req:Request,id:string,body:UpdateContentDto){await this.admin(req);const result=await this.pool.query(`update content_entries set title=coalesce($1,title),summary=coalesce($2,summary),body=coalesce($3,body),status=coalesce($4,status),published_at=case when $4='PUBLISHED' and published_at is null then now() when $4='DRAFT' then null else published_at end,version=version+1,updated_at=now() where id=$5 and version=$6 returning *`,[body.title?.trim(),body.summary?.trim(),body.body?.trim(),body.status,id,body.expectedVersion]);if(!result.rowCount)throw new BadRequestException("Konten berubah atau tidak ditemukan.");return result.rows[0]}async plans(){return{items:(await this.pool.query(`select code,name,audience,price_amount,currency,interval,features,status from subscription_plans where status in ('ACTIVE','COMING_SOON') order by audience,price_amount`)).rows}}}
@Controller("content")export class ContentController{constructor(private readonly content:ContentService){}@Get()list(){return this.content.published()}@Get(":slug")entry(@Param("slug")slug:string){return this.content.entry(slug)}}
@Controller("admin/content")export class AdminContentController{constructor(private readonly content:ContentService){}@Get()list(@Req()req:Request){return this.content.adminList(req)}@Post()create(@Req()req:Request,@Body()body:CreateContentDto){return this.content.create(req,body)}@Patch(":id")update(@Req()req:Request,@Param("id")id:string,@Body()body:UpdateContentDto){return this.content.update(req,id,body)}}
@Controller("subscription-plans")export class PlansController{constructor(private readonly content:ContentService){}@Get()list(){return this.content.plans()}}
