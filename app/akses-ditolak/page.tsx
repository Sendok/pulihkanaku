import Link from"next/link";import{ShieldX}from"lucide-react";import{requireAccount,roleHome}from"@/lib/account";
export const dynamic="force-dynamic";
export default async function AccessDenied(){const account=await requireAccount("/akses-ditolak");return<main className="auth-shell"><section className="auth-card"><div className="auth-icon danger"><ShieldX/></div><p className="eyebrow">Akses dibatasi</p><h1>Halaman ini tidak sesuai dengan role akunmu.</h1><p>Akun <strong>{account.email}</strong> terdaftar sebagai <strong>{account.role}</strong>. Pemeriksaan ini dilakukan di server.</p><Link className="primary-button full auth-submit" href={roleHome(account.role)}>Kembali ke dashboard akun</Link><Link className="outline-button full" href="/signout-with-chatgpt?return_to=/">Keluar dan gunakan akun lain</Link></section></main>}
