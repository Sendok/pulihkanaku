"use client";import{ArrowRight,LockKeyhole,WalletCards}from"lucide-react";import{useState}from"react";
export function PayoutForm(){const[loading,setLoading]=useState(false);const[error,setError]=useState("");const[done,setDone]=useState<{bankCode:string;lastFour:string}|null>(null);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError("");const form=new FormData(event.currentTarget);try{const response=await fetch("/api/v1/payout-accounts",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({bankCode:String(form.get("bankCode")),accountName:String(form.get("accountName")),accountNumber:String(form.get("accountNumber"))})});const result=await response.json()as{success:boolean;data?:{bankCode:string;lastFour:string};error?:{message:string}};if(!response.ok||!result.success||!result.data)throw new Error(result.error?.message??"Rekening belum dapat disimpan.");setDone(result.data)}catch(cause){setError(cause instanceof Error?cause.message:"Rekening belum dapat disimpan.")}finally{setLoading(false)}}if(done)return <div className="onboarding-success"><WalletCards/><h2>Rekening tersimpan</h2><p>{done.bankCode} •••• {done.lastFour} sedang menunggu verifikasi nama pemilik. Nomor lengkap tidak pernah ditampilkan kembali.</p><a className="primary-button" href="/app">Kembali ke dashboard <ArrowRight/></a></div>;return <form className="onboarding-form" onSubmit={submit}><div className="auth-field"><label htmlFor="bankCode">Bank atau dompet digital</label><select id="bankCode" name="bankCode" required defaultValue=""><option value="" disabled>Pilih tujuan payout</option>{["BCA","BNI","BRI","MANDIRI","BSI","JAGO","SEABANK","DANA","GOPAY","OVO","SHOPEEPAY"].map(x=><option key={x}>{x}</option>)}</select></div><div className="auth-field"><label htmlFor="accountName">Nama pemilik rekening</label><input id="accountName" name="accountName" required minLength={3} autoComplete="name"/></div><div className="auth-field"><label htmlFor="accountNumber">Nomor rekening/akun</label><input id="accountNumber" name="accountNumber" required inputMode="numeric" pattern="[0-9 ]{8,24}" autoComplete="off"/></div><div className="security-callout"><LockKeyhole/><span><strong>Dienkripsi sebelum disimpan.</strong>Admin hanya melihat empat digit terakhir. Perubahan rekening selalu masuk antrean verifikasi ulang.</span></div>{error&&<div className="auth-error">{error}</div>}<button className="primary-button full auth-submit" disabled={loading}>{loading?"Mengenkripsi dan menyimpan…":<>Simpan rekening payout <ArrowRight/></>}</button></form>}
