"use client";import{useMemo,useState}from"react";const money=new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0});export function IncomePlanner(){const[shifts,setShifts]=useState(3);const[pay,setPay]=useState(150000);const[weeks,setWeeks]=useState(4);const estimate=useMemo(()=>shifts*pay*weeks,[shifts,pay,weeks]);return <section className="income-planner"><div><label>Shift per minggu <strong>{shifts}</strong><input type="range" min="1" max="7" value={shifts} onChange={e=>setShifts(Number(e.target.value))}/></label><label>Rata-rata bayaran per shift <strong>{money.format(pay)}</strong><input type="range" min="50000" max="500000" step="25000" value={pay} onChange={e=>setPay(Number(e.target.value))}/></label><label>Periode rencana <strong>{weeks} minggu</strong><input type="range" min="1" max="12" value={weeks} onChange={e=>setWeeks(Number(e.target.value))}/></label></div><aside><span>Estimasi skenario</span><strong>{money.format(estimate)}</strong><small>{shifts*weeks} shift dalam {weeks} minggu</small><p>Ini alat perencanaan, bukan jaminan. PulihkanAku tidak mengenakan biaya kepada worker untuk melamar.</p></aside></section>}
