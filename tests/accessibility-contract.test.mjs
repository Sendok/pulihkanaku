import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";
test("operational UI exposes semantic live, status, error, and labelled form states",async()=>{const[messaging,operations,css]=await Promise.all([readFile(new URL("../app/_operations/messaging-console.tsx",import.meta.url),"utf8"),readFile(new URL("../app/admin/operations/operations-console.tsx",import.meta.url),"utf8"),readFile(new URL("../app/globals.css",import.meta.url),"utf8")]);for(const token of ['aria-live="polite"','role="alert"','htmlFor="message-body"','aria-label="Kirim pesan"'])assert.match(messaging,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")));assert.match(operations,/role="tablist"/);assert.match(operations,/aria-selected/);assert.match(css,/prefers-reduced-motion/);assert.match(css,/\.sr-only/)});
